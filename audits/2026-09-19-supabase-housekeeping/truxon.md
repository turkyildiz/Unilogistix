# Truxon — Supabase house-cleaning audit (read-only, static)

Audited: `/home/ike/work/truxon` @ `main` (local HEAD `425c42d8`; local `main` was
3 commits behind `origin/main` at audit time — `git fetch`/`pull` was
deliberately not run to keep this audit read-only; re-run against a fresh
checkout if those 3 commits touch `supabase/`).

`/home/ike/work/truxon-wt/` holds 19 separate worktrees on `claude/*` feature
branches (not `main`) — several are actively working the same territory as
this checklist (RLS posture, service-role bless flow, tenant catch-up,
QuickManage recovery). Not audited in depth; listed for context only, see
"Overlap with in-house tooling" below.

No secret values were read or printed. No `supabase` CLI link/deploy/db
commands were run. No live DB access.

## Project(s)

- Local CLI alias only: `project_id = "truxon"` in `supabase/config.toml:2` — not a real ref.
- Real prod/HQ project ref **`okoeeyxxvzypjiumraxq`** — scattered across ~12 deploy scripts/docs rather than one canonical place: `deploy/hq-bootstrap/secrets.env.example:23`, `deploy/edi/wrangler.toml:17`, `deploy/forest-voice/announcer/README.md:30`, `deploy/devbox/MIGRATION.md:75`, `deploy/shield-visual/.env.example:2`, `deploy/tenant-provision/shield-live-check.sh:12,15`, `deploy/doc-reader/.env.example:3`, `deploy/vision-enrich/README.md:31`, `deploy/tolls/README.md:42,59`, `deploy/fuel/README.md:35,39`, `deploy/whatsapp-shadow/.env.example:3`.
- Demo/drill tenant project ref **`hrqycojgdlzbxwizxyhj`** — `deploy/tenant-provision/drill-demo-spa.md:43,81`.
- Template/example tenant ref **`aehuvwocnghsiizwhsmv`** — `deploy/tenant-provision/provision.sh:335,443`, `scripts/go-live.sh:133`.
- Credential locations (paths only, no values) documented in `deploy/secrets/INVENTORY.md` — Truxon/Supabase section (service_role, CRON_SECRET, DB password, anon key → KeePassXC vault); a separate `Truxon/Freightex` section explicitly calls out Freightex as **its own, separate Supabase project**.
- Architecture note: this looks like one Supabase project per tenant (`hq_tenants`, `hq-*` functions), so "the Truxon project" is really a family of projects; the audit above covers the schema/migrations shared across them.

Migrations: **1,131 files**, `20260715200001` → `20260918150000` (2026-07-15 to 2026-09-18). Edge functions: **78**, matching **78/78** `[functions.*]` entries in `supabase/config.toml` — zero drift.

## Checklist

| # | Item | Status | Evidence | Recommended fix | Severity |
|---|------|--------|----------|------------------|----------|
| 1 | Service-role key in client bundle | OK | No `service_role`/`SUPABASE_SERVICE_ROLE_KEY`/`sb_secret_`/hardcoded `eyJ` JWT found under frontend/mobile/webos/tv source. `frontend/src/changelog.generated.ts` mentions the word "service_role" in prose only. `frontend/.env.example` / `.env.drill.local.example` hold only placeholder `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`. Frontend bundle was **not built** (skipped, static grep only per instructions) | Keep a CI grep step (`service_role`, `sb_secret_`, `eyJ`) over built bundles as belt-and-suspenders | Low (residual: NEEDS LIVE CHECK if founder wants bundle-level confirmation) |
| 2 | RLS enabled with effectively-`true` policies | OK | Only 6 `using (true)` hits, all `for select to authenticated` (not anon) on shared reference tables, each commented as intentional: `supabase/migrations/20260715200003_rls.sql:22`, `20260715230001_company_settings.sql:22`, `20260721090001_map_pois.sql:17`, `20260721110001_truck_parking.sql:21`, `20260729000050_off_duty_tetris.sql:54`, `20260818040100_integration_library.sql:30` | None required; re-review if any of these tables later grow sensitive columns | Low |
| 2b | RLS enabled but not FORCEd (owner/cron/dashboard SQL bypasses policy; service_role bypasses regardless) | **ISSUE** | 230 tables have `alter table ... enable row level security` (e.g. `supabase/migrations/20260715200003_rls.sql:5-7`); **0** have a matching `alter table ... force row level security` anywhere in `supabase/migrations/*.sql`. 224 `cron.schedule` call-sites run as the migration-runner/owner role. Large volume of `security definer` functions (hundreds of grep hits) — each owned by the table owner would become newly subject to RLS the moment FORCE is enabled on its target table | Enable FORCE table-by-table, starting with highest-sensitivity tables; before flipping, inventory every SECURITY DEFINER function owned by the table owner and re-test/re-policy it — this repo already has a self-verifying migration pattern (see `20260918100000_...sql`) to copy for the rollout. service_role callers are unaffected either way | **High** |
| 3 | FK columns without indexes | NEEDS LIVE CHECK | 307 `references` vs 260 `create index` statements across 1,131 files — not fully reconciled by hand at this file count. Spot checks: `loads.customer_id` is indexed (`supabase/migrations/20260715200001_schema.sql:164`); `documents` uses a polymorphic `entity_type/entity_id` composite index instead of a per-FK index | Run the `db-audit.sql` §3 query (FK-without-index) live, or Supabase's own Performance Advisor | Medium |
| 4 | Public storage buckets | OK | `personal`/`team` buckets (`supabase/migrations/20260716230001_drives.sql:9-13`, `public=false`) and `db-backups` (`20260720670001_db_backup.sql:5-6`, `public=false`, comment: "No storage policies: only service_role touches the bucket") | None required | Low |
| 5 | Triggers assuming single-row changes | OK | 95 `create trigger` statements; 92 `FOR EACH ROW`, 4 `FOR EACH STATEMENT` — all 4 are deliberate bulk-DML/ransomware guards and an immutable audit log, not accidental single-row assumptions: `supabase/migrations/20260722003001_ransomware_guard.sql:112`, `20260722006001_dml_ransomware_guard.sql:115,119`, `20260722002001_security_audit_log.sql:45`. `on_auth_user_created` is correctly `FOR EACH ROW` (`20260715200001_schema.sql:51-55`) | None required | Low |
| 6 | Migration drift (hand-run prod changes / dashboard-only objects not in repo) | OK, with one confirmed self-caught instance | 78/78 edge functions declared in `config.toml`, matching directory exactly. No "ran directly in prod" / "via Studio" language found in HANDOFF.md, `deploy/migration-its/*.md`, `deploy/tenant-provision/*.md`. **Positive finding**: `supabase/migrations/20260918100000_revoke_authenticated_default_privileges.sql` (most recent migration, 2026-09-18) found and fixed a case where `authenticated`/`anon` had silently inherited TRUNCATE/REFERENCES/TRIGGER (and in one case full INSERT/UPDATE/DELETE) via Supabase's default `alter default privileges` bootstrap, surviving earlier narrower `revoke all` statements — exactly the drift class this checklist looks for, already caught and fixed both retroactively and structurally | Keep the `alter default privileges ... revoke ...` guard as new tables are added; re-run a similar sweep periodically | Medium (recurring-risk pattern, not currently open) |
| 7 | anon key → Supabase Auth directly, bypassing app rate limits | NEEDS LIVE CHECK | `config.toml` `[auth]` (`enable_signup = false`) blocks public self-signup at the local-stack level; `[auth.email]` `enable_signup = true` allows admin-created accounts to log in with password. File's own comment (`config.toml:24-33`) states this file governs the **local stack only** — the hosted project's Auth rate-limit settings are separate and unconfirmed from the repo | Confirm hosted Supabase dashboard → Authentication → Rate Limits are set (not left at defaults), independent of any app-level limiter | Medium |
| 8 | Backups never restore-tested | OK, real pipeline exists | `deploy/backup/restore_test.sh` spins up a throwaway `postgres:17-alpine` container, restores the latest backup, counts accounts/profiles/loads/customers/documents/storage objects/migrations, prints `PASS: restore verified` (`restore_test.sh:97-100`); `deploy/backup/run-restore-test.sh` logs to `logs/restore_test_$(date +%Y%m).log`; `deploy/backup/dr-drill.sh` runs weekly ("Sunday, after restore_test") and checks log freshness per backup source (`dr-drill.sh:12,93-115`) | None required in-repo; confirm live scheduling (see live-check list) | Low (contingent on live-check #3 below) |
| 9 | Connection pooling fit | OK | No `DATABASE_URL`, `pg.Pool`, `node-postgres`, or `postgres.js` usage found under `supabase/functions/**` — all DB access goes through supabase-js/PostgREST/RPC, so PgBouncer-transaction-mode/prepared-statement breakage does not apply | None required | Low |
| 10 | Cron jobs / edge functions not in repo | OK, with a volume caveat | 78/78 functions match `config.toml`, zero drift. **224** `cron.schedule` call-sites across migrations (many are repeated re-schedules of the same small job set as SQL bodies changed over time) | Live-check `cron.job` for orphaned/duplicate schedule names left behind by superseded migrations | Medium |
| 11 | Multiple tables claiming the same concept | NEEDS LIVE CHECK | `drivers` vs `eld_drivers`/`eld_driver_status` (internal record vs ELD-provider mirror — plausible legitimate split, but a report could accidentally query the wrong one). 11+ load-related tables (`loads`, `load_stops`, `load_stop_events`, `load_stop_eld`, `load_line_items`, `load_accessorials`, `load_checkcalls`, `load_templates`, `load_share_links`, `load_feedback`, `tracking_load_assignments`) plus a `load_problems` view — normal parent/child design, not obvious duplication, but worth confirming all dashboards (`dashboard_v2`/`dashboard_wtd`/`dashboard_yoy` migrations) source "active load" counts from the same table | Live-check that all "active X" dashboards/reports agree on source table; no schema change indicated | Low |
| 12 | Auth triggers only for password signup (OAuth/magic link/phone skipped) | OK by design; NEEDS LIVE CHECK for hosted config | `on_auth_user_created` (`supabase/migrations/20260715200001_schema.sql:51-55`) fires on **any** `auth.users` insert regardless of signup method — provisioning itself is method-agnostic. `config.toml` only configures `[auth.email]`; no `[auth.external.*]`/`[auth.sms]` blocks exist in-repo, and the file states it governs the local stack only | Confirm in the hosted dashboard that no OAuth/phone provider was hand-enabled outside this repo's single-email-flow assumption | Medium |

## Additional / bonus findings (outside the numbered checklist)

- **Honeypot tables**: `api_keys`, `bank_accounts`, and `_hp_*` functions are deliberate decoys (`supabase/migrations/20260722001001_honeypots.sql:122,140`, `20260722001002_honeypot_grants.sql:7-8`, `20260726000014_honeypot_view_invoker.sql:21-22`). Confirm they hold no real data and are excluded from any reporting/backup restore-count logic.
- **`gmail-grant-probe`** is the one edge function with `verify_jwt = false` and **no** inline justification comment (every other `verify_jwt=false` entry in `config.toml` is commented). Minor documentation gap; confirm its actual in-function auth gate.
- **Overlap with in-house tooling**: the `truxon-wt/` worktrees (`bless-svc`, `catchup`, `git-posture`, `posture`, `truncate-fix`, `attach-av`, `health`/`diagnose`/`smoke`, `qm-recover`/`qm-restore`/`recover`) plus `app_private.security_posture()`/`security_baseline` (referenced in `CHANGELOG.md` and the `20260918100000` migration) suggest Team Truxon already runs an active in-house security-posture pipeline covering items 2, 2b, and 3. Check whether it already produces a better live report than this static audit before building new tooling on top of it.

## Needs-live-check list for the founder

1. Confirm the canonical prod project ref (`okoeeyxxvzypjiumraxq`) and consider adding a one-line pointer to it in `deploy/secrets/INVENTORY.md` instead of only the ~12 scattered deploy-script references.
2. Confirm hosted Supabase dashboard Auth settings (OAuth/phone providers, signup rate limits) — `config.toml` only documents the local dev stack.
3. Confirm `dr-drill.sh` / `run-restore-test.sh` are actually scheduled (cron/systemd timer) on the NAS and their most recent logs show `PASS`, not just that the scripts exist and work when run by hand.
4. Before enabling `FORCE ROW LEVEL SECURITY` (item 2b): inventory every SECURITY DEFINER function owned by the table owner per table, pilot FORCE on 2-3 low-risk tables first.
5. Verify `gmail-grant-probe`'s actual in-function auth gate.
6. Run `select jobid, schedule, command, username from cron.job order by jobid;` (see `db-audit.sql`) to spot orphaned/duplicate schedules given 224 `cron.schedule` call-sites in migration history.
7. Run the FK-without-index query in `db-audit.sql` §3 live — static reconciliation of 307 `references` vs 260 `create index` statements wasn't feasible by hand.
8. Confirm honeypot tables (`api_keys`, `bank_accounts`) contain no real data.
9. Run `db-audit.sql` §2b (FORCE / BYPASSRLS / cron owners / SECURITY DEFINER owners) live to get the actual owner and BYPASSRLS role list, which is not statically determinable.
